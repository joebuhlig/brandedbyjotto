<% if defined?(image) && image %>
  <%= image_tag image.attachment.url(:product), :itemprop => "image", class: "product_image", data: {zoom_image: image.attachment.url(:large)} %>
<% else %>
  <%= product_image(@product, :itemprop => "image", class: "product_image") %>
<% end %>